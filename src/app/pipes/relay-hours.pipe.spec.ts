import { RelayHoursPipe } from './relay-hours.pipe';

describe('RelayHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new RelayHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
