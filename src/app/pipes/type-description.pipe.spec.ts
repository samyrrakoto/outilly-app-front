import { TypeDescriptionPipe } from './type-description.pipe';

describe('TypeDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
